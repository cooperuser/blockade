const {Block} = require("blockade/blocks/block");
const {Standard} = require("blockade/blocks/standard");

const blocks = {Block, Standard};

function newBlock(type, game, position) {
	return new Standard(game, position);
}

module.exports = {Block, blocks, newBlock};