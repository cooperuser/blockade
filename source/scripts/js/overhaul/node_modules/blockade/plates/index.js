const {Plate} = require("blockade/plates/plate");
const {Standard} = require("blockade/plates/standard");

const plates = {Plate, Standard};

function newPlate(type, game, position) {
	return new Standard(game, position);
}

module.exports = {Plate, plates, newPlate};